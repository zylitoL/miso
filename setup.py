from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Model to predict future electric grid loads from electric vehicles',
    author='MISO',
    license='',
)
